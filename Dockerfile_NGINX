# Utilisez l'image de base officielle Nginx à partir du Docker Hub
FROM nginx

# Exposez le port 80 pour permettre l'accès aux requêtes HTTP
EXPOSE 80

# CMD est la commande qui sera exécutée lorsque le conteneur est démarré à partir de l'image
CMD ["nginx", "-g", "daemon off;"]